import './ProductDetail.css'

const ProductDetail = (product) => {
  return (
    <div className='productDetails'>Product detail information will be shown here, but I have no way to put props into this component</div>
  )
}

export default ProductDetail